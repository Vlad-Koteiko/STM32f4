
#include "can.hpp"


namespace drivers::can
{
    Can::Can(ADDRESSES_CAN address) : baseAddress(address)
    {
        
    }
    
    bool Can::start()
    {
        
    }
    
    bool Can::stop()
    {
        
    }
}